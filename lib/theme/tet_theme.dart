import 'package:flutter/material.dart';

/// Màu và style chủ đạo Tết: đỏ, vàng, nền ấm
class TetTheme {
  TetTheme._();

  static const Color redPrimary = Color(0xFFC41E3A);   // Đỏ Tết
  static const Color redDark = Color(0xFF8B0000);      // Đỏ đậm
  static const Color gold = Color(0xFFD4AF37);          // Vàng kim
  static const Color goldLight = Color(0xFFFFE55C);    // Vàng sáng
  static const Color cream = Color(0xFFFFF8E7);        // Kem
  static const Color bgGradientStart = Color(0xFF1a0a0a);
  static const Color bgGradientEnd = Color(0xFF4a1515);
}
